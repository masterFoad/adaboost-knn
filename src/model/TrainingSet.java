package model;

import java.util.Map;

public class TrainingSet {

    private Tuple[] dataVector;


    //what if each node has a sorted array of all the points.
    //the first k are the k that we need.
    //instead of array we can use a minimum heap,
    //so each node will have a minimum heap with euclidean distance comparator.


}
